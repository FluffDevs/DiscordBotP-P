{
  "name": "discord-bot-pp-v2",
  "version": "0.1.0",
  "description": "V2 du bot Discord — vérification via composants natifs (select menu, modals)",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon --watch src --exec node src/index.js",
    "lint": "eslint . --ext .js",
    "deploy-commands": "node src/deploy-commands.js",
    "deploy-if-ready": "node scripts/deploy-if-ready.js"
  },
  "engines": {
    "node": ">=16.9.0"
  },
  "dependencies": {
    "@discordjs/rest": "^2.6.0",
    "discord-api-types": "^0.38.33",
    "discord.js": "^14.12.0",
    "dotenv": "^16.0.0",
    "node-telegram-bot-api": "^0.63.0"
  },
  "devDependencies": {
    "eslint": "^9.39.1",
    "nodemon": "^3.1.11"
  }
}
