# Exemple .env
# Mettez ici le token du bot Telegram et l'ID du chat/groupe où envoyer les logs
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrSTUvwxYZ
TELEGRAM_CHAT_ID=-1001234567890

# Intervalle en secondes pour regrouper les logs avant envoi (défaut 15)
TELEGRAM_BATCH_INTERVAL_SEC=15
TELEGRAM_ENABLED=false

# Variables existantes du bot Discord (exemples)
DISCORD_TOKEN=your_discord_token_here
PREFIX=!
# ----------------------------
# .env.example pour Peluche Bot
# Copiez ce fichier en `.env` et remplacez les valeurs par vos propres secrets/valeurs.
# Pour les messages multi-lignes, utilisez \n comme séparateur de saut de ligne.
# Les variables listées ci-dessous correspondent aux options et textes personnalisables
# utilisés par le bot. Si une variable n'est pas définie, le code utilise une valeur
# par défaut sûre.
# ----------------------------

# ----------------------------
# Secrets & options de base
# ----------------------------
DISCORD_TOKEN=your-bot-token-here	# (obligatoire) token du bot
PREFIX=!                             # préfixe pour les commandes text (ex: !say)
CLIENT_ID=your-application-client-id  # utilisé pour le déploiement des slash-commands
GUILD_ID=your-development-guild-id   # optionnel : si renseigné, déploiement instantané sur ce guild

# ----------------------------
# Channels / IDs / Roles
# ----------------------------
# Forum channel où les threads de vérification seront créés. Peut être l'ID ou le nom exact.
FORUM_CHANNEL_ID=your-forum-channel-id

# Rôles (id ou nom). Laisser vide si vous ne souhaitez pas appliquer ces rôles automatiquement.
NON_VERIFIED_ROLE=Non vérifié
PELUCHER_ROLE=Peluche
VERIFIER_ROLE=Modérateur
# Rôle optionnel "artiste" : indiquez soit l'ID numérique du rôle, soit la mention <@&ID>, soit le nom exact du rôle.
# Exemples :
# ARTIST_ROLE=123456789012345678
# ARTIST_ROLE=<@&123456789012345678>
# ARTIST_ROLE=Artiste

# ----------------------------
# Vérification : contenu et questions
# ----------------------------
# Deux modes possibles pour la vérification envoyée en DM :
# 1) QUESTIONS : JSON string contenant un tableau de questions (ancienne méthode)
#    Exemple : QUESTIONS='["Présente-toi","Quel âge as-tu ?"]'
# 2) VERIF_MESSAGE_MD : message markdown complet envoyé au membre. Si défini,
#    le bot enverra ce texte tel quel et collectera les messages jusqu'à 'done'.
#    Exemples de retours à la ligne : utilisez \n pour séparer les lignes.
QUESTIONS=
VERIF_MESSAGE_MD=

# Instruction envoyée après VERIF_MESSAGE_MD pour expliquer comment terminer
VERIF_DONE_INSTRUCTION=Merci : réponds à ces questions dans ce DM. Tape `done` quand tu as fini (ou attends 5 minutes).\nRéponds en un ou plusieurs messages.

# Message de greeting en mode question-par-question (fallback si VERIF_MESSAGE_MD absent)
VERIF_DM_GREETING=Bonjour {username} ! Voici le message de vérification — merci d'y répondre.

# ----------------------------
# Messages personnalisables (texte affiché aux utilisateurs)
# Vous pouvez utiliser les placeholders suivants dans ces messages :
#   {username}   -> pseudo de l'utilisateur (sans discriminator)
#   {userId}     -> identifiant Discord de l'utilisateur
#   {guild}      -> nom du serveur (guild)
#   {justification} -> texte fourni par un modérateur lors d'un refus
# Remarque : le code applique des interpolations simples pour certains messages,
# voir README ou demandez-moi si vous voulez une fonction de template plus complète.
# ----------------------------

# Identifiant du propriétaire (optionnel) — utilisé pour autorisations OWNER-only
OWNER_ID=

# Ping
PING_REPLY=Pong 🏓(!)
PING_REPLY_SLASH=Pong 🏓 (slash)

# Commande say (messages d'erreur / confirmations)
SAY_NO_PERMISSION=Vous n'êtes pas autorisé à utiliser cette commande.
SAY_NO_MESSAGE=Veuillez fournir un message à envoyer.
SAY_ERROR_PREFIX=Erreur lors de l'envoi:
SAY_SENT=Message envoyé.
SAY_WEBHOOK_SENT=Message envoyé (via webhook).
SAY_NO_CMD=Aucune commande fournie.
SAY_CMD_NOT_FOUND=Commande introuvable:
SAY_CMD_FILE_INVALID=Le fichier de commande n'expose pas une fonction execute.
SAY_EXEC_DONE=Commande exécutée.
SAY_EXEC_ERROR=Erreur lors de l'exécution:

# /msgverif (commande qui poste le bouton de demande de vérif)
MSGVERIF_NO_PERMISSION=Vous n'êtes pas autorisé à utiliser cette commande.
MSGVERIF_BUTTON_LABEL=Recevoir le message de vérification
MSGVERIF_PROMPT=Cliquez sur le bouton ci-dessous pour recevoir le message de vérification en DM.

# Réponses et actions lors de la vérification (acceptation / refus)
VERIF_PENDING_MESSAGE=Une vérification est déjà en cours pour toi — vérifie tes DMs.
VERIF_DM_SENT=Le message de vérification t'a été envoyé en DM (si tes DMs sont ouverts).
VERIF_ACCEPTED_CHANNEL=✅ Vérification acceptée par {moderator} — rôle appliqué à <@{userId}>.
VERIF_REJECT_REQUEST=<@{userId}> Merci de fournir une justification du refus en répondant dans ce fil. Vous avez 30 minutes.
VERIF_REJECTED_DM=Votre vérification a été refusée sur {guild}. Raison donnée par l'équipe :\n\n{justification}
VERIF_REJECTED_CHANNEL=Refus enregistré par {moderator} et transmis au membre.

# ----------------------------
# Remarques finales
# - Copiez ce fichier en .env avant de lancer le bot.
# - Pour inclure des retours à la ligne dans VERIF_MESSAGE_MD, utilisez \n.
# - Si vous voulez des templates plus avancés (ex: conditionnels), je peux
#   ajouter une petite fonction utilitaire pour remplacer automatiquement les
#   placeholders dans tout le code de vérification.
# ----------------------------

