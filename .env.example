# .env.example pour Peluche Bot
# Copiez ce fichier en `.env` et remplacez les valeurs par vos propres secrets/valeurs.
# Pour les messages multi-lignes, utilisez \n comme séparateur de saut de ligne.
# Les variables listées ci-dessous correspondent aux options et textes personnalisables
# utilisés par le bot. Si une variable n'est pas définie, le code utilise une valeur
# par défaut sûre.

# ----------------------------
# 1) ESSENTIEL (obligatoire)
# - Ces variables doivent être configurées pour que le bot fonctionne.
# ----------------------------
DISCORD_TOKEN=your-bot-token-here	# (obligatoire) token du bot (remplacez par votre token privé dans .env, ne commitez pas)
FORUM_CHANNEL_ID=1409264764575551699   # channel forum (ID numérique ou nom exact) où poster les vérifications
PREFIX=!                             # préfixe pour les commandes text (ex: !say)

# ----------------------------
# 2) RECOMMANDÉ (importants mais pas strictement obligatoires)
# - Ces variables permettent d'activer le comportement de vérification complet
#   et d'autoriser/limiter qui peut valider.
# ----------------------------
# Rôles (id ou nom). Laisser vide si vous ne souhaitez pas appliquer ces rôles automatiquement.
NON_VERIFIED_ROLE=1409253783200071701
PELUCHER_ROLE=1363472160110284870
PELUCHES_ROLE=    # alias historique accepté par le code (PELUCHES_ROLE)
PELUCHER=         # autre alias/fallback possible
VERIFIER_ROLE=1439047400193790152
ARTIST_ROLE=1396838062612414565
ARTIST_ROLE_ID=   # alternative acceptée (ID numérique)
ARTIST_ROLE_TAG=  # alternative acceptée (mention <@&ID>)
MAJOR_ROLE=Adulte                         # (optionnel) rôle à appliquer si le membre est majeur
MAJOR_ROLE_ID=    # alias/ID numérique pour MAJOR_ROLE
MAJEUR_ROLE=      # alias FR possible (MAJEUR_ROLE)
MINOR_ROLE=Mineur                         # (optionnel) rôle à appliquer si le membre est mineur
MINOR_ROLE_ID=    # alias/ID numérique pour MINOR_ROLE
MINEUR_ROLE=      # alias FR possible (MINEUR_ROLE)

CLIENT_ID=1438875669009272834  # utilisé pour le déploiement des slash-commands
DEPLOY_ALL_GUILDS=true
# (optionnel) si vous préférez déployer dans un seul guild pour le dev
GUILD_ID=

# ----------------------------
# 3) TELEGRAM / LOGS (optionnel mais recommandé pour la supervision)
# ----------------------------
# Mettez TELEGRAM_ENABLED=true pour activer l'envoi des logs vers Telegram
TELEGRAM_ENABLED=true
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here  # token du bot Telegram (laisser vide si vous n'utilisez pas Telegram)
TELEGRAM_CHAT_ID=-5013714173
TELEGRAM_BATCH_INTERVAL_SEC=15
LOG_LEVEL=debug  # debug|info|warn|error — niveau de logs (utilisé par src/logger.js)

# ----------------------------
# 4) STARTUP / RETRY (optionnel)
# ----------------------------
STARTUP_MAX_RETRIES=5
STARTUP_INITIAL_BACKOFF_MS=2000
STARTUP_BACKOFF_MULTIPLIER=2
STARTUP_RESTART_ENABLED=true   # true|false — autoriser le redémarrage automatique (startup/start-and-monitor.js)
# Backwards-compatible aliases (le code accepte aussi ces anciens noms)
MAX_RETRIES=
INITIAL_BACKOFF_MS=
BACKOFF_MULTIPLIER=

# ----------------------------
# 5) Vérification : contenu et questions (optionnel)
# - Deux modes possibles pour la vérification envoyée en DM :
#   1) QUESTIONS : JSON string contenant un tableau de questions (ancienne méthode)
#      Exemple : QUESTIONS='["Présente-toi","Quel âge as-tu ?"]'
#   2) VERIF_MESSAGE_MD : message markdown complet envoyé au membre.
#      Si VERIF_MESSAGE_MD est défini, le bot enverra ce texte tel quel et collectera
#      les réponses jusqu'à ce que l'utilisateur tape `done` ou que le timeout soit atteint.
# Pour les retours à la ligne dans VERIF_MESSAGE_MD, utilisez \n.
QUESTIONS="**Bonjour ! Comment vas-tu ?\nPeux-tu te présenter rapidement ?**\n\n• Es-tu Furry ? Depuis combien de temps ? \n• Comment as-tu découvert ce serveur ? \n• Es-tu un artiste qui propose des commissions ?\n• Es-tu majeur ou mineur ?\n• Fais-nous une petite présentation de toi :3\n"
VERIF_MESSAGE_MD=

VERIF_DONE_INSTRUCTION=Merci : réponds à ces questions dans ce DM. Tape `done` quand tu as fini (ou attends 5 minutes).\nRéponds en un ou plusieurs messages.
VERIF_DM_GREETING=Bonjour {username} ! Voici le message de vérification — merci d y répondre.

# ----------------------------
# 6) Messages personnalisables (texte affiché aux utilisateurs)
# Vous pouvez utiliser les placeholders suivants dans ces messages :
#   {username}   -> pseudo de l'utilisateur (sans discriminator)
#   {userId}     -> identifiant Discord de l'utilisateur
#   {guild}      -> nom du serveur (guild)
#   {moderator}  -> pseudo du modérateur
#   {justification} -> texte fourni par un modérateur lors d'un refus
# ----------------------------
# Identifiant du propriétaire (optionnel) — utilisé pour autorisations OWNER-only
OWNER_ID=

# Ping
PING_REPLY=Pong 🏓(!)
PING_REPLY_SLASH=Pong 🏓 (slash)

# Commande say (messages d'erreur / confirmations)
SAY_NO_PERMISSION=Vous n'êtes pas autorisé à utiliser cette commande.
SAY_NO_MESSAGE=Veuillez fournir un message à envoyer.
SAY_ERROR_PREFIX=Erreur lors de l'envoi:
SAY_SENT=Message envoyé.
SAY_WEBHOOK_SENT=Message envoyé (via webhook).
SAY_NO_CMD=Aucune commande fournie.
SAY_CMD_NOT_FOUND=Commande introuvable:
SAY_CMD_FILE_INVALID=Le fichier de commande n'expose pas une fonction execute.
SAY_EXEC_DONE=Commande exécutée.
SAY_EXEC_ERROR=Erreur lors de l'exécution:

# /msgverif (commande qui poste le bouton de demande de vérif)
MSGVERIF_NO_PERMISSION=Vous n'êtes pas autorisé à utiliser cette commande.
MSGVERIF_BUTTON_LABEL=Recevoir le message de vérification
MSGVERIF_PROMPT=Cliquez sur le bouton ci-dessous pour recevoir le message de vérification en DM.

# Réponses et actions lors de la vérification (acceptation / refus)
VERIF_PENDING_MESSAGE=Une vérification est déjà en cours pour toi — vérifie tes DMs.
VERIF_DM_SENT=Le message de vérification t'a été envoyé en DM (si tes DMs sont ouverts).
VERIF_ACCEPTED_CHANNEL=✅ Vérification acceptée par {moderator} — rôle appliqué à <@{userId}>.
VERIF_REJECT_REQUEST=<@{userId}> Merci de fournir une justification du refus en répondant dans ce fil. Vous avez 30 minutes.
VERIF_REJECTED_DM=Votre vérification a été refusée sur {guild}. Raison donnée par l'équipe :\n\n{justification}
VERIF_REJECTED_CHANNEL=Refus enregistré par {moderator} et transmis au membre.

# ----------------------------
# Remarques finales
# - Copiez ce fichier en .env avant de lancer le bot.
# - Pour inclure des retours à la ligne dans VERIF_MESSAGE_MD, utilisez \n.
# - Les rôles peuvent être fournis comme ID (préféré), mention <@&ID> ou nom exact.
# - Vérifiez que le rôle du bot a la permission Manage Roles et est au-dessus des rôles cibles.
# ----------------------------

